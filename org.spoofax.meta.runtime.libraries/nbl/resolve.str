module nbl/resolve

imports
  
  nbl/tasks
	nbl/entries
	task/core
      
rules // Reference resolution
  
  nabl-is-reference =
    ?Use(_)
    
  nabl-has-reference =
    collect-one(nabl-is-reference)
    
  nabl-has-reference =
    has-annos;
    get-annos;
    nabl-has-reference
  
  // TODO: use task/utils collect-all-annos
	nabl-collect-all-resolved-defs:
		ast -> def*
		with
			if ast' := <insert-results> ast then
				def* := <collect-all(?Def(_), union)> ast'
			else
				def* := []
			end

	// TODO: use task/utils collect-one-annos
	nabl-collect-one-resolved-def =
		insert-results-or-create-dependency;
		oncetd-annos((); ?d); !d
		
	oncetd-annos(s) = s <+ has-annos; get-annos; one(oncetd-annos(s)) <+ one(oncetd-annos(s))
	