module libanalysis2/export/export

imports

  libanalysis2/positioning/-
  signatures/libanalysis2/namebinding/-
  pp/libanalysis2/namebinding/-

rules

  calculate-simple-positions =
      collect-all(?DeclId(_,_,<id>) <+ ?RefId(_,_,<id>))
    ; qsort((position-to-key,position-to-key);lt)
    ; map-with-index(Swap)

  apply-simple-positions(|sp) = bottomup(try(<lookup> (<is-ASTPos>,sp)))
 
rules
